---
description: Commit current worktree changes and fast-forward merge into main
agent: build
subtask: true
model: github-copilot/claude-haiku-4.5
---

Use the `ship-worktree` skill to commit current worktree changes and fast-forward merge into main. If the user passed $ARGUMENTS, use that as the commit message.
